dialog (Result) {
  match: NumberAndPlace(this){
    from-output: PlayJobsDone
  }

    template ("There are #{value(MagicNumber)} #{value(Object)} in the #{value(Place)}") {
      speech ("<speak><lang xml:lang=\"en-GB\" voice=\"M02\">#{value(NumCount)}... There are #{value(MagicNumber)} #{value(Object)} in the #{value(Place)}</lang></speak>")
  }
}
